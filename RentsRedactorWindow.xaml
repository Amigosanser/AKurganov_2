<Window x:Class="AKurganov_2.RentsRedactorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование аренды"
        Height="450" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        Closing="Window_Closing">

    <Window.Resources>
        <ResourceDictionary Source="Dictionary.xaml"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Редактирование аренды" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Комната:" 
                       Width="120" 
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
            <ComboBox x:Name="cbApartment" 
                      Width="200"
                      DisplayMemberPath="RoomInfo"
                      SelectionChanged="cbApartment_SelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Посетитель:" 
                       Width="120" 
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
            <ComboBox x:Name="cbVisitor" 
                      Width="200"
                      DisplayMemberPath="FIO"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Сотрудник:" 
                       Width="120" 
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
            <ComboBox x:Name="cbStaff" 
                      Width="200"
                      DisplayMemberPath="StaffInfo"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Количество дней:" 
                       Width="120" 
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
            <TextBox x:Name="txtDays" 
                     Width="200"
                     Text="1"
                     PreviewTextInput="txtDays_PreviewTextInput"
                     TextChanged="txtDays_TextChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Стоимость:" 
                       Width="120" 
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
            <TextBlock x:Name="txtCost" 
                       Width="200"
                       VerticalAlignment="Center"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="Green"/>
        </StackPanel>

        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button x:Name="btnSave" 
                    Content="Сохранить" 
                    Width="100" 
                    Height="35"
                    Margin="10"
                    Click="btnSave_Click"/>
            <Button x:Name="btnCancel" 
                    Content="Отмена" 
                    Width="100" 
                    Height="35"
                    Margin="10"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>